---
output:
  redis:
    enabled: true
    host: "redis"
    port: 6379
    Index: "turnbeat"
  stdout:
    enabled: true
filter:
  filters: ["graphite"]
input:
  tcp_2014:
    enabled: true
    port: 2014
    type: "carbon"
